console.log(history);

// history.length (property) => total history elements
console.log(history.length);

// history object methods
// back()
history.back(); /* prev page */

// forward()
history.forward(); /* next page */

// go()
history.go(3); /* 3 pages forward */
history.go(-2); /* 2 pages backward */