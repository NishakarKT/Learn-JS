// BOM => Browser-Object Module => Browser manipulation

let iHeight = window.innerHeight;
let oHeight = window.outerHeight;
let iWidth = window.innerWidth;
let oWidth = window.outerWidth;

getSize();

function getSize(){
    console.log(iHeight);
    console.log(oHeight);
    console.log(iWidth);
    console.log(oWidth);
}
